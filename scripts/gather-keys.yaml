---
- name: Collect SSH host keys for every target
  hosts: localhost
  gather_facts: false

  vars:
    target_hosts: "{{ groups['workers'] }}"

  tasks:
    - name: ====== Scan and add worker SSH keys to known_hosts ======
      ansible.builtin.shell: "ssh-keyscan -T 2 {{ item }} >> ~/.ssh/known_hosts 2>/dev/null || true"
      loop: "{{ target_hosts }}"
      changed_when: false
