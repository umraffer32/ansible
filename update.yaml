---
- name: Update and upgrade all packages on worker nodes
  hosts: all
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Perform full upgrade
      apt:
        upgrade: full
        autoremove: yes
        autoclean: yes
